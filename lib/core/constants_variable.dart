class ConstantsVariable {
  //app name
  static const String appName = 'Pro Multimedia';

  /// animation initial delay  for AnimationHelper
  static const Duration initialDelayAnimation = Duration(milliseconds: 300);

  /// duration
  static const Duration animationDurationS = Duration(seconds: 300);
  static const Duration animationDurationM = Duration(milliseconds: 600);
  static const Duration animationDurationL = Duration(milliseconds: 1000);
  static const Duration animationDurationXL = Duration(milliseconds: 1500);

  ///shimmer  - długość animacji
  static const Duration shimmerDuration = Duration(milliseconds: 1500);
}
